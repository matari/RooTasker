import type { BaseSchedule } from "../../../../src/shared/ProjectTypes";

export interface Schedule extends BaseSchedule {
  // Add any webview-specific fields here if needed in the future.
  // For now, it directly uses all fields from BaseSchedule.
}
